# purr0 SPEC

Tiny, frozen subset of Purr used for bootstrap. This spec must remain stable once the first compiler is generated from it.

Features: minimal expressions, integer arithmetic, function calls, simple types.
