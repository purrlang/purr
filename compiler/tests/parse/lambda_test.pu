// lambda_test.pu - parser test: simple lambdas and invocation

func apply_to_x(f: (i32): i32, v: i32) -> i32 {
  return f(v);
}

func main() -> i32 {
  let inc = (x: i32): i32 => x + 1;
  let a: i32 = inc(5);
  let b: i32 = apply_to_x((n: i32): i32 => n * 2, 6);
  return a + b; // 6 + 12 = 18
}
