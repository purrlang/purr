# purrc

Self-hosted compiler implementation of Purr (`purrc`).

Layout:
- `main.pu` — compiler entry point
- `parse/` — parser
- `ast/` — AST nodes and utilities
- `emit/` — codegen (e.g., `llvm.pu`)

Status:
- Stubbed; do not implement features not present in spec.
