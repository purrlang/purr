// parser.pu - minimal parser API for purr0 (stub)

// Public API (to be implemented):
// parse_program(source: string) -> Program

// A tiny, hand-parseable interface to allow early tests and incremental
// implementation. For now this file contains stubs and documentation; later it
// will contain the actual parsing code that constructs AST nodes declared in
// `../ast`.
//
// Parsing must follow SPEC.md (namespace/use, var/assign/for/switch, interfaces,
// actors with fields, methods via Type.Method, enums, and spawn expressions).
// Keywords are fixed in KEYWORDS.md.

func parse_program(source: string) -> string {
  // Stub: parser not yet implemented.
  // For the initial bootstrap we may return a serialized representation (JSON
  // or s-expression) to simplify testing and iteration.
  return "<parsed-program-placeholder>";
}
